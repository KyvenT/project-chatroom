import '@emotion/react'

declare module '@emotion/react' {
  export interface Theme {
    colors: {
    dark_grey: string,
    light_grey: string,
    white: string,
    brown: string,
    orange: string,
    }
  }
}